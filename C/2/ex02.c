#include "file.h"

// ecrire un programme qui appelle la fonction temp() contenue dans file.h
// la fonction prend comme parametre l'adresse d'un int

// avec la valeur il faut creer un CAN pour la convertir en temperature
// il faut creer une boucle infini pour demender les valeur puis demender la val de temp() la convertir puis ecrire dans le terminal le resulta puis faire une pause de 1s pour recommencer
